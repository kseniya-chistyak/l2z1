#include <stdio.h>
#include <cstring>
 
int main ()
{
    char* str = "spring";
    int i, len;
    len = strlen(str);
    char* str1 = new char [len];
    for (i=0; i<len; i++)
        str1[i] = str[len-i-1];
    str1[i]='\0';
    puts (str1);
    return 0;
}
