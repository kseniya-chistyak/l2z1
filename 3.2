#include <stdio.h> // подключаем библиотеку
int main ()// точка входа в программу(начало программы)
{ int f;//объявляем переменную
FILE *test_file;// навзание файла
test_file = fopen ("test","r");//открытие файла
for (int i = 1; i <= 3; i++)//используем цикл for(инициализация начального значения int i = 1,
//задаем условие при каждом шаге цикла i <= 3,приращениекак будет изменяться значение
//в каждом шаге цикла,шаг составляет единицу) ) 
{ fscanf (test_file,"%d\n", &f);//чтение из потока test_file строку и значение переменной f 
printf ("%d. %d \n", i, f); }//данная функции записывает значение аргументов
fclose ;//открытие файла закрытие файла
return 0;
}
