#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a[10] = {1, 8, 3, 6, 5, 4, 9, 2, 8, 12};
    int n = sizeof(a)/sizeof(a[0]);
    sort(a, a+n, greater<int>());
    cout << " \n";
    for (int i = 0; i < n; ++i)
      cout << a[i] << " ";
    return 0;
}
